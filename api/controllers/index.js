const CatalogProductsController = require('./catalog_products.controller')
const SessionTokenController = require('./session_token.controller')

module.exports = {
    CatalogProductsController,
    SessionTokenController
}